const job = "developer";

const usuario = {
    firstName: 'Samuel',
    lastName: 'Crespo',
    age: 24,
    job: job,
}

console.log(`Mi nombre es ${usuario.firstName} ${usuario.lastName}, tengo ${usuario.age} años y trabajo como ${usuario.job}`);
